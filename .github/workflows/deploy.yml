# https://github.com/actions/deploy-pages#usage
name: Deploy MinePlan to GitHub Pages
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      # Pick your own package manager and build script
      - run: npm install
      - name: 🗑 クリーンアップ
        run: rm -rf .nuxt

      - name: ⚙️ Nuxt の準備（.nuxt/tsconfig.json を生成）
        run: |
          npx nuxi prepare
          ls -la .nuxt
          cat .nuxt/tsconfig.json || echo "⚠️ .nuxt/tsconfig.json がありません！"
      # - run: npx nuxi prepare
      - run: npx nuxt build
      - run: npx nuxt generate && npx gh-pages -d ./app/.output/public
      # - name: Upload artifact
      #   uses: actions/upload-pages-artifact@v3
      #   with:
      #     path: ./app/.output/public
  # Deployment job
  deploy:
    # Add a dependency to the build job
    needs: build
    # Grant GITHUB_TOKEN the permissions required to make a Pages deployment
    permissions:
      pages: write # to deploy to Pages
      id-token: write # to verify the deployment originates from an appropriate source
    # Deploy to the github_pages environment
    environment:
      name: github_pages
      url: ${{ steps.deployment.outputs.page_url }}
    # Specify runner + deployment step
    runs-on: ubuntu-latest
    steps:
      - name: Deploy MinePlan to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
